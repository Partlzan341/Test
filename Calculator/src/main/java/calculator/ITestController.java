package calculator;

public interface ITestController {
    boolean isNumber(String number);

    boolean isAction(String action);

    boolean checkNumber(String inputNumber);

    boolean checkAction(String action);
}
